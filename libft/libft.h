#ifndef LIBFT_H
# define LIBFT_H

// coloquei isso pq se não ele diz que desconhece o size_t
# include <stdlib.h>
#include <ctype.h>

size_t	ft_strlen(const char *s);
int		ft_isdigit(int cs);



#endif